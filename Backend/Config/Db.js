// ⚠️ IMPORTANTE: Este archivo está siendo reemplazado por ../Db/connection.js
// Se mantiene temporalmente para compatibilidad, pero se recomienda usar ../Db/connection.js

const pool = require('../Db/connection');

module.exports = pool;

